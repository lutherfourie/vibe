# Windows Daemon for Vibe Self-Build Loop

1. Install NSSM or use Go service wrapper.
2. Run `vibe serve --daemon` (new flag) or use PM2 / Task Scheduler.
3. Remote control via dashboard API or WebSocket.

UI updates added: Online status + live log feed for loop.

Tested and pushed.